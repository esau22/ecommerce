import FormAuth from "@/components/shared/form/form-auth";

const Auth = () => {
  return (
    <section className="h-full flex flex-col items-center justify-normal">
      <FormAuth />
    </section>
  );
};

export default Auth;
